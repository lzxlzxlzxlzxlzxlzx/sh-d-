# 数独求解器

一个基于 Vue 3 的 9×9 数独求解 Web 应用。用户可直接在网页上填写已知数字，点击「求解」获取答案。

## 功能

- 在 9×9 网格中直接输入已知数字（1-9）
- 一键求解数独
- 支持键盘方向键在单元格间导航
- 原题数字与求解结果用不同颜色区分
- 输入冲突检测

## 运行

```bash
npm install
npm run dev
```

在浏览器中打开显示的本地地址（通常是 http://localhost:5173）即可使用。

## 构建

```bash
npm run build
```

构建产物在 `dist` 目录。
